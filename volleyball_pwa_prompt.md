# バレーボール・スコア記録PWA 開発プロンプト

以下の要件をすべて満たす、バレーボール用スコア記録PWA（単一のHTMLファイル）を作成してください。

## 1. 基本コンセプト
- **レスポンシブ・レイアウト**:
    - **横画面（Landscape）**: 左右にチームA・Bのスコアを配置。
    - **縦画面（Portrait）**: 上下にチームA・Bのスコアを配置。背後から見ている人への視認性を高めるため、フォントサイズを極大化すること。
- **デザイン**: 屋外でも見やすい高コントラストなダークモード（背景：黒、文字：黄色・白）。

## 2. スコアリング・設定機能
- **操作性**: 画面の半分（上下または左右）を巨大なタップエリアとし、叩くだけで加点。
- **設定変更**: チーム名、セット数（1, 3, 5）、各セットの目標点数（15, 25等）をユーザーが自由に変更可能。
- **コートチェンジ**: 配置（上下または左右）をワンタップで入れ替える機能。
- **ロジック**: デュース対応、Undo（1手戻る）機能、タイムアウト回数の管理。

## 3. 得点履歴（タイムライン）表示【最重要要件】
- **ビジュアル表示**: 各セットごとに、得点の推移を「横並びのボックス形式」で視覚化すること。
    - 得点したチーム側に、その時点の累積点数ボックスを表示。
    - 連続得点した場合は水平にボックスを連結。
    - タイムアウト（T）を取ったタイミングを、その時点の点数と合わせて列の中に挿入。
- **参照タイミング**: 
    - **試合中**: 現在進行中のセットおよび前のセットのタイムラインを即座に確認できる画面。
    - **過去の履歴**: 保存された過去の試合ログからも、この詳細なタイムライン画面を完全に再現・参照できること。

## 4. データ保存・運用機能
- **データ永続化**: `localStorage` を使用し、ブラウザのリフレッシュや終了後も試合データ・設定・履歴が保持されること。
- **ログ機能**: 試合終了時に「日付・対戦相手・各セットの最終スコア・詳細タイムライン」を履歴リストとして保存。
- **デバイス最適化**:
    - **Wake Lock API**: 記録中に画面がスリープするのを防止。
    - **Vibration API**: タップ時に短い振動フィードバックを返し、ブラインド操作をサポート。

## 5. 出力形式
- HTML、CSS (Tailwind CSS CDN利用可)、JavaScriptをすべて1つのHTMLファイルに統合。
- PWA化のための `manifest.json` と `Service Worker` を登録するスクリプトを含めること。
